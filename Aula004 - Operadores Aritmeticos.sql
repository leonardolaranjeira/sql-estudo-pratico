
/*
    OPERADORES ARITMÉTICOS
    + = SOMAR
    - = SUBTRAIR
    * = MULTIPLICAR
    / = DIVIDIR
    % = MÓDULO

    EXEMPLOS ABAIXO:
 */

    SELECT 1 + 3 AS ADICAO,
           5 - 10 AS [SUB(-1)],
           1700 + (-900) AS NEG,
           5 * (5 + 1) AS MULT_SOM,
           -5 * (500 / 40) % 3 AS TODO,
           '5' + 3 AS CONCAT